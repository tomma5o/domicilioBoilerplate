export const NumberResults = ({ results }) => {
	return (
        <div class="w-full text-center">
            {results && (

                <span>numero risultati: {results}</span>
            )}
        </div>
	);
};
